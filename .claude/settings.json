{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=\"$(jq -r '.tool_input.file_path // empty')\"; case \"$FILE\" in *.astro|*.ts|*.tsx|*.mjs) OUT=\"$(npm run --silent typecheck 2>&1)\" || jq -n --arg out \"$OUT\" '{hookSpecificOutput:{hookEventName:\"PostToolUse\",additionalContext:(\"Typecheck failed:\\n\" + $out)}}';; esac",
            "timeout": 30,
            "statusMessage": "Type-checking (astro check)"
          }
        ]
      }
    ]
  }
}
